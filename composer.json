{
    "name": "allv01d/occterm",
    "description": "Run occ commands from a web terminal, as an administrator",
    "license": "AGPL-3.0-or-later",
    "type": "project",
    "config": {
        "optimize-autoloader": true,
        "sort-packages": true
    },
    "require": {
        "php": "^8.1"
    },
    "require-dev": {
        "psr/log": "^3.0",
        "symfony/console": "^6.4.41"
    },
    "scripts": {
        "test": "php tests/run-tests.php"
    }
}
